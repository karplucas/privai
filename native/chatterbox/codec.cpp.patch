diff --git a/cmake/SetupTtsBackbone.cmake b/cmake/SetupTtsBackbone.cmake
index f88f7dc..e2adfb0 100644
--- a/cmake/SetupTtsBackbone.cmake
+++ b/cmake/SetupTtsBackbone.cmake
@@ -1,3 +1,6 @@
+# Vulkan for the ISOLATED backbone, independent of codec.cpp's own ggml.
+option(CODEC_BACKBONE_VULKAN "Build the tts backbone with Vulkan" OFF)
+
 # SetupTtsBackbone.cmake — build an ISOLATED llama.cpp backbone for tts-cli.
 #
 # codec.cpp links ggml 0.9 (the standalone ggml submodule) as a shared
@@ -18,6 +21,28 @@
 
 include(ExternalProject)
 
+# The backbone is a nested CMake project, so it does NOT inherit the toolchain
+# of the outer build.  Cross-compiling (Android NDK, iOS) has to forward it
+# explicitly or the ExternalProject silently builds for the host and the link
+# fails on incompatible architectures.
+set(_ll_cross_args "")
+if(CMAKE_TOOLCHAIN_FILE)
+    list(APPEND _ll_cross_args "-DCMAKE_TOOLCHAIN_FILE=${CMAKE_TOOLCHAIN_FILE}")
+endif()
+foreach(_v ANDROID_ABI ANDROID_PLATFORM ANDROID_STL ANDROID_NDK
+           CMAKE_SYSTEM_NAME CMAKE_SYSTEM_VERSION CMAKE_ANDROID_ARCH_ABI
+           CMAKE_OSX_SYSROOT CMAKE_OSX_ARCHITECTURES)
+    if(DEFINED ${_v})
+        list(APPEND _ll_cross_args "-D${_v}=${${_v}}")
+    endif()
+endforeach()
+
+# GGML_NATIVE probes the *build* machine's ISA, which is meaningless and often
+# fatal when the target is a phone.
+if(CMAKE_CROSSCOMPILING)
+    list(APPEND _ll_cross_args "-DGGML_NATIVE=OFF")
+endif()
+
 set(_ll_dir      "${CMAKE_CURRENT_SOURCE_DIR}/common/third-party/llama.cpp")
 set(_ll_prefix   "${CMAKE_BINARY_DIR}/llama-static")
 set(_ll_libdir   "${_ll_prefix}/src/llama-build/src")
@@ -48,11 +73,31 @@ set(_ll_archives
     "${_ll_ggmldir}/libggml-cpu.a"
     "${_ll_ggmldir}/libggml-base.a")
 
+# The Vulkan backend is a separate archive; without it the wrapper link
+# fails on an undefined ggml_backend_vk_reg (the registry in libggml.a
+# references it unconditionally once the backend is compiled in).
+set(_ll_omp_lib "-lgomp")
+if(ANDROID OR APPLE)
+    # The NDK has no libgomp; ggml falls back to its own threading there.
+    set(_ll_omp_lib "")
+endif()
+
+set(_ll_extra_libs "")
+if(CODEC_BACKBONE_VULKAN)
+    # The bundled ggml-vulkan archive needs the loader at link time.
+    list(APPEND _ll_extra_libs "-lvulkan")
+endif()
+
+if(CODEC_BACKBONE_VULKAN)
+    list(APPEND _ll_archives "${_ll_ggmldir}/ggml-vulkan/libggml-vulkan.a")
+endif()
+
 ExternalProject_Add(llama_static
     SOURCE_DIR      "${_ll_dir}"
     PREFIX          "${_ll_prefix}"
     BINARY_DIR      "${_ll_prefix}/src/llama-build"
     CMAKE_ARGS
+        ${_ll_cross_args}
         -DCMAKE_BUILD_TYPE=Release
         -DBUILD_SHARED_LIBS=OFF
         -DCMAKE_POSITION_INDEPENDENT_CODE=ON
@@ -65,7 +110,7 @@ ExternalProject_Add(llama_static
         -DLLAMA_CURL=OFF
         -DLLAMA_OPENSSL=OFF
         -DGGML_NATIVE=ON
-        -DGGML_VULKAN=OFF
+        -DGGML_VULKAN=${CODEC_BACKBONE_VULKAN}
         -DGGML_BACKEND_DL=OFF
     BUILD_COMMAND   ${CMAKE_COMMAND} --build <BINARY_DIR> --target llama-common --config Release
     BUILD_BYPRODUCTS ${_ll_archives}
@@ -92,7 +137,7 @@ add_custom_command(
     COMMAND ${CMAKE_CXX_COMPILER} -shared -fPIC -o "${TTS_BACKBONE_SO}"
             -Wl,--whole-archive ${_ll_archives} -Wl,--no-whole-archive
             -Wl,--version-script=${_ll_vscript}
-            -lpthread -lm -ldl -lgomp
+            -lpthread -lm -ldl ${_ll_omp_lib} ${_ll_extra_libs}
     DEPENDS llama_static ${_ll_archives} "${_ll_vscript}"
     COMMENT "Linking isolated llama backbone → libttsbackbone.so (ggml_* hidden)"
     VERBATIM)
diff --git a/common/tts_runner.cpp b/common/tts_runner.cpp
index f702e70..03c8d77 100644
--- a/common/tts_runner.cpp
+++ b/common/tts_runner.cpp
@@ -1095,7 +1095,10 @@ bool tts_runner_synthesize(const tts_runner_params & a, tts_runner_result * out)
     // ── Backbone init ──────────────────────────────────────────────
     llama_backend_init();
     llama_model_params mp = llama_model_default_params();
-    mp.n_gpu_layers = 0;
+    // Upstream pins this to 0 on the assumption the backbone is small
+    // enough not to need offload.  For Chatterbox T3 it is the whole AR
+    // loop, so honour the caller's use_gpu instead.
+    mp.n_gpu_layers = a.use_gpu ? 999 : 0;
     llama_model * lmodel = llama_model_load_from_file(a.backbone_path.c_str(), mp);
     if (!lmodel) {
         out->error = "llama_model_load_from_file failed: " + a.backbone_path;
diff --git a/src/lm/lm.cpp b/src/lm/lm.cpp
index c0c3d07..2442224 100644
--- a/src/lm/lm.cpp
+++ b/src/lm/lm.cpp
@@ -129,6 +129,11 @@ bool codec_lm_check_unfused_audio_tables(
         if (!tied_heads) {
             std::snprintf(buf, sizeof(buf), "lm.heads_%zu.weight", i);
             ggml_tensor * t_h = ggml_get_tensor(lm->codec->weights, buf);
+            if (t_h == nullptr && i == 0) {
+                // Older converters (the published Chatterbox multilingual
+                // GGUFs) name the single head lm.c0_head.weight.
+                t_h = ggml_get_tensor(lm->codec->weights, "lm.c0_head.weight");
+            }
             if (t_h == nullptr) {
                 lm->last_error = std::string("missing tensor: ") + buf;
                 return false;
diff --git a/src/lm/parallel_heads_delay.cpp b/src/lm/parallel_heads_delay.cpp
index 054916e..eb943b3 100644
--- a/src/lm/parallel_heads_delay.cpp
+++ b/src/lm/parallel_heads_delay.cpp
@@ -211,6 +211,10 @@ bool init(codec_lm * lm) {
         } else {
             std::snprintf(buf, sizeof(buf), "lm.heads_%d.weight", i);
             impl->heads[(size_t) i] = ggml_get_tensor(lm->codec->weights, buf);
+            if (impl->heads[(size_t) i] == nullptr && i == 0) {
+                impl->heads[0] = ggml_get_tensor(lm->codec->weights,
+                                                 "lm.c0_head.weight");
+            }
         }
         if (impl->heads[(size_t) i] == nullptr || impl->audio_embds[(size_t) i] == nullptr) {
             // Already validated via codec_lm_check_unfused_audio_tables, but
